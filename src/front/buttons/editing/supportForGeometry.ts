export const getCxCyCircle = (elem, classElem?: string) => {
    /*
    let elemArr: any = document.querySelectorAll('.' + classElem)
    let cx: string
    let cy: string

    for (let i = 0; i < elemArr.length; i++) {
        let elem = elemArr[i]
        elemArr[i].addEventListener('click', getCxCy.bind(null, elem))

    }
    */
    //console.log(elemArr[0])
    //return elemArr[0]
    console.log('cx - ' + elem)
}

const getCxCy = (elem) => {
    //cx = elem.getAttribute('cx')
    //cy = elem.getAttribute('cy')
    //console.log('cx - ' + elem)
}
